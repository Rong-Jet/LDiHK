# API Contract Pointer

This file is intentionally not an API specification.

The authoritative API shape for frontend and backend integration lives in:

```text
../docs/backend/frontend-api-spec.md
```

Do not duplicate endpoint request or response schemas here. Frontend requirements
should link to the canonical contract and only document UI-specific behavior.
